@model IEnumerable<FitnessHubStays.Models.BookingActivityDto>
@{
    ViewBag.Title = "View";
}


<div class="container mt-5">
    <div class="row">
        <div class="col">
            <h2>Booked Activities</h2>
        </div>
        <div class="col mb-3 d-flex justify-content-end">
            <a href="/BookingActivity/Add" class="btn btn-success">Add New Activity</a>
        </div>
    </div>

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Booking</th>
                <th>Activity</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var bookingactivity in Model)
            {
                <tr>
                    <td>@bookingactivity.BookingID</td>
                    <td>@bookingactivity.ActivityName</td>

                    <td>
                        <a href="/Room/View/" class="btn btn-primary" aria-label="View Room Details"><i class="bi bi-eye"></i> View</a>
                        <a href="/Room/Edit/" class="btn btn-warning" aria-label="Edit Room Details"><i class="bi bi-pencil"></i> Edit</a>
                        <a href="/Room/Delete/" class="btn btn-danger" aria-label="Delete Room Details"><i class="bi bi-trash"></i> Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


